if (document.readyState === 'complete') {
    console.log("removeItem condition met");
    return a + b;
    error.textContent = '';
    error.textContent = '';
} else {
    console.log("Condition not met");
}